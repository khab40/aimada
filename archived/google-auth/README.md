# Google Auth archive

Archived Google OAuth frontend provider, identity/session UI, FastAPI routes, JWT/session storage, backend tests, ADR, splash asset, and isolated styles.

This module is not mounted or compiled. The active application runs without user authentication or application sessions.
